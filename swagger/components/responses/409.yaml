description: Register error - Email in use
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 409
        message:
          type: string
          example: 'RegisterError'
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Email is already used'
